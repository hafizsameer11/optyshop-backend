// This file is deprecated - all admin routes moved to /api/admin/*
// Public campaign route is now at /api/campaigns
// This file kept for backward compatibility but should be removed after migration

const express = require('express');
const router = express.Router();

// Empty router - all routes moved
module.exports = router;
